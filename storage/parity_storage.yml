apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: parity-disk
  annotations:
    volume.beta.kubernetes.io/storage-class: default
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi